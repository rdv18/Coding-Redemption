
Given a singly linked list of size N. 
The task is to left-shift the linked list by k nodes, where k is a given positive integer smaller than or equal to length of the linked list.


class Solution{
    //Function to rotate a linked list.
    public Node rotate(Node head, int k) {
        // add code here
        int count=0;
        k++;
        Node prev=null;
        Node resHead=head;
        Node start=head;
        while(head!=null)
        {
            count++;
        
            if(count==k)
            {
                resHead=head;
                prev.next=null;
            }
            prev=head;
            head=head.next;
        }
        if(resHead!=start)  //when resHead has changed then point last node to first node
        {
            prev.next=start;
        }
        
        
        return resHead;

    }
}
